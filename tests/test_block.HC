// Test des opcodes 0x40-0x48 (contexte de bloc)
I64 bh   = BlockHash(Number() - 1);
I64 cb   = Coinbase();
I64 ts   = Timestamp();
I64 num  = Number();
I64 rnd  = PrevRandao();
I64 gl   = GasLimit();
I64 cid  = ChainId();
I64 sbal = SelfBalance();
I64 bf   = BaseFee();
